apiVersion: v1
kind: Service
metadata:
  name: devops-studio-app
  namespace: devops-studio
  labels:
    app: devops-studio-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: devops-studio-app

